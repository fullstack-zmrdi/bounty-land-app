language: node_js
node_js:
  - "7.8.0"

install:
  - npm install
  - npm install -g react-native-cli

script:
  - npm run lint
  - npm run flow
  - npm test
  - npm run bundle:all

branches:
  only:
    - master

notifications:
  email:
    - martycakjakub@gmail.com
